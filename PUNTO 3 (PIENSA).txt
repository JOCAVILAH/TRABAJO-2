Funcion resultado = piensa (mensaje) // funcion piensa arroja un valor de retorno llamada resultado
	a=Aleatorio(1,50)// funcion que escoje un numero aleatorio entre un intervalo, ese numero se almacena en la variable a 
	i=0 // contador definido en 0 para controlar el numero de veces que se repite el ciclo "repetir"
	Repetir
		Escribir " Ingresa un numero entre 1 y 50 "
		leer b // variable ingresada por el usuario 1 intento de adivinar
		si b>=0 y b<=50 entonces// si el valor ingresado por el usuario esta en el intervalo analizar
			Si b<a Entonces// 1 caso (numero ingresado es menor a numero aleatorio
				imprimir ("El numero que buscas es mayor");
					SiNo
						Si b>a Entonces// 2 caso (numero ingresado es mayor a numero aleatorio
						imprimir ("El que buscas en menor");
							SiNo// 3 caso (numero ingresado es igual al numero aleatorio
								imprimir ("ADIVINASTE");
								resultado=1// Valor de retorno de la funcion para frenar el ciclo "repetir"
						Fin Si
			Fin Si
			i=i+1// contador para aumentar 1 a 1 
		SiNo
			imprimir ("Numero no valido"); // esta linea es por si no se cumple el intervalo solicitado por la maquina
		fin si
	Hasta Que i==5 o resultado==1 //Fin del ciclo repetir hasta que el contador sea igual a 5, es decir 5 intentos sin adivinar 
	//o hasta que la variable de retorno resultado sea igual a 1, es decir que adivino el numero antes de los 5 intentos
	si resultado <>1 entonces// esto indica que no advino el numero
		Imprimir (" Se acabaron los intentos PERDISTE ")
	FinSi
Fin Funcion


Algoritmo punto_3
	
	Imprimir piensa("")
	
FinAlgoritmo
